#!/bin/bash

# This script executes a command on a VM and returns its output
# Example Command:
# ansible MongoDB -i .vagrant/provisioners/ansible/inventory/vagrant_ansible_inventory --private-key=.vagrant/machines/MongoDB/virtualbox/private_key -u vagrant -m shell -a "~/break.sh"

echo "This script does nothing yet..."
