from os.path import dirname, abspath, join

# Virtual machine related constants
VM_BASE_DIR = '/opt/VMs'

# Rugby constants
SCRIPTS_DIR = dirname(abspath(join(__file__, 'shell_scripts')))
TEMPLATES_DIR = dirname(abspath(join(__file__, 'templates')))

